meta {
  name: flush cache
  type: http
  seq: 7
}

post {
  url: {{url}}/v1/flush
  body: json
  auth: inherit
}

body:json {
  {
    "confirm": true
  }
}

tests {
  test("200 OK", function () {
    expect(200).to.equal(res.status);
  })
  
  test("Correct Number of Entries Removed", function () {
    expect("2").to.equal(res.body.numberOfEntriesRemoved);
  })
}

settings {
  encodeUrl: true
}
