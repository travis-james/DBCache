docker run --name postgres-db \
  -e POSTGRES_PASSWORD=mypassword \
  -e POSTGRES_USER=myuser \
  -e POSTGRES_DB=mydatabase \
  -p 5432:5432 \
  -v postgres-data:/var/lib/postgresql/data \
  -d postgres

Environment variables:
POSTGRES_PASSWORD: Sets the password for the PostgreSQL superuser (required).
POSTGRES_USER: Creates a new superuser with this name (defaults to "postgres" if not specified).
POSTGRES_DB: Creates a new database with this name (defaults to the username if not specified).
Port mapping (-p 5432:5432) maps the container's PostgreSQL port (5432) to the same port on your host machine, allowing you to connect to PostgreSQL using localhost:5432.
Volume mounting (-v postgres-data:/var/lib/postgresql/data) creates a Docker volume named postgres-data that persists the database files outside the container. This ensures your data isn't lost when the container stops or is removed.
The -d flag makes sure the container runs in detached mode. In plain English, this means the container runs in the background.

docker build -t my-image-name -f /path/to/Dockerfile .
docker run --name my-container -d my-image-name
docker run --name redis-con -d redis-img


=== Creating redis data
docker run --name my-redis -d -p 6379:6379 redis:8
docker run --rm --network container:my-redis redis redis-cli -h localhost SET users:4 '{"id":4, "name":"Nishiwaki", "email":"aoaoan@neo.net", "age":49}'
docker run --rm --network container:my-redis redis redis-cli -h localhost SET users:1 '{"id":1, "name":"Alice", "email":"alice@example.com", "age":30}'

docker run --rm --network container:dbcache_redis redis redis-cli -h localhost GET users:1

docker exec -it my-redis redis-cli BGSAVE
docker exec -it my-redis redis-cli CONFIG GET dir
docker cp my-redis:/data/dump.rdb .




=== Creating postgres data
docker run --rm --network container:dbcache_postgres postgres psql -h localhost -U local_dev -d local_db -c "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT, email TEXT, age INT);"

docker run --rm --network container:dbcache_postgres postgres psql -h localhost -U local_dev -d local_db -c "INSERT INTO users (name, email, age) VALUES ('Alice', 'alice@example.com', 30);"

docker run --rm --network container:dbcache_postgres postgres psql -h localhost -U local_dev -d local_db -c "INSERT INTO users (name, email, age) VALUES ('Oomoto', 'nocchi@perfume.com', 36);"

docker run --rm --network container:dbcache_postgres postgres psql -h localhost -U local_dev -d local_db -c "INSERT INTO users (name, email, age) VALUES ('Kashino', 'kashiyuka@europe.gov', 22);"

docker run --rm --network container:dbcache_postgres postgres psql -h localhost -U local_dev -d local_db -c "INSERT INTO users (name, email, age) VALUES ('Nishiwaki', 'aoaoan@neo.net', 49);"

docker exec -t dbcache_postgres pg_dump -U local_dev -d local_db > dump.sql



protoc --go_out=. --go-grpc_out=. --proto_path=. dbcache.proto

pb.go contains all the protocol buffer code to populate, serialize, and retrieve request and response message types.
grpc.pb.go is service interface, defines what I need to implement, and client stubs.

Next Steps
1. Implementing the service interface generated from our service definition: doing the actual “work” of our service.
2. Running a gRPC server to listen for requests from clients and dispatch them to the right service implementation.


EXPLAINER:
Why Do You Extend UnimplementedDBCacheServiceServer?
Instead of implementing DBCacheServiceServer directly, the recommended practice is to embed UnimplementedDBCacheServiceServer into your own Server struct:
This ensures that if future versions of the .proto file introduce new RPC methods, your code won't break due to missing implementations—you'll just inherit empty method stubs.

Then, you override only the methods you need,

PutQueryResult is for manually caching known results, DB hits, or prewarmed data—no DB touch.

GetCachedQuery is the only one (in that model) that can be DB-aware, if you include fallback_query.