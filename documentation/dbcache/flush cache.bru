meta {
  name: flush cache
  type: http
  seq: 7
}

post {
  url: {{url}}/v1/flush
  body: json
  auth: inherit
}

body:json {
  {
    "confirm": true
  }
}

tests {
  expect(res.status).to.equal(200);
  
  expect(res.body.numberOfEntriesRemoved).to.equal("2");
  
}

settings {
  encodeUrl: true
}
